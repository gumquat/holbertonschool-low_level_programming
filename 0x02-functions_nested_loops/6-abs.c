/**
*_abs - computes absolute value of an integer
*@i: is a variable
*Return: something lol
*/

#include "main.h"

int int_abs(int i)
{
	if (i > 0)
	{
		return (n);
	}
	else if (i < 0)
	{
		return (-n);
	}
	else
	{
		return (i);
	}
}
