/**
*_abs - computes absolute value of an integer
*@i: is a variable
*Return: something lol
*/

#include "main.h"

int _abs(int i)
{
if (i > 0)
{
return (i);
}
else if (i < 0)
{
return (-i);
}
else
{
return (i);
}
}
