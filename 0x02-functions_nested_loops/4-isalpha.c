/**
*_isalpha - checks for alphabet character C
*@i: is a thing
*Return: true if is letter
*/
#include "main.h"
int functionNAME(int i)
{
char letter = 'c';
if (i >= 'a' && i <= 'z')
{
	return (1);
}

else if (i >= 'A' && <= 'Z')
{
	return (1);
}

else
{
	return (0);
}

}
