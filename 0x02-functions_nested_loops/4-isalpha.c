/**
*int_isalpha - description HERE
*@i: is a thing
*Return: true if c is letter
*/
#include "main.h"
int functionNAME(int i)
{
char letter = 'c';
if (i >= 'a' && i <= 'z')
{
return (1);
}
{
else if (i >= 'A' && <= 'Z')
}
else
{
return (0);
}
}
