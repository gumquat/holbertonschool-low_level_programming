/**
*main - PUT DESCRIPTION HERE
*Return: main always 0
*/

#include <stdio.h>
#include <string.h>

int main(void)
{
int i;
char p;
for (i = 0; i <= 9; i++)
{
putchar(i);
}
for (p = 'a'; p <= 'f'; p++)
{
putchar(p);
}
putchar('\n');
return (0);
}
